// const double = (x: number | string) => {
//   if (typeof x === "number") {
//     return x * 2;
//   }
//   return `${x}-${x}`;
// };

// console.log(double(36));
// console.log(double("Adriaan"));

type Person = {
  name: string;
  age: number;
};

let Jaqub: Person = {
    name: "Jaqub",
    age: 26
}
